\section{The Pipeline: Natural Language \texorpdfstring{$\to$}{→} Network \texorpdfstring{$\to$}{→} Formalization}
\label{sec:pipeline}

% This is the methodological core of the paper, distinguishing from all existing tools.

\subsection{Phase 1: Natural Language \texorpdfstring{$\to$}{→} Structured Network}
\label{sec:pipe-phase1}

% Math text (textbook/paper/notes)
%   ↓ AI-assisted + manual editing
% Astrolabe obj/mor network (MDX content + typed sort + hash)
%
% Users input mathematical content in natural language (MDX)
% AI skills (/add-node, /add-edge, /analyze-structure) assist generating obj/mor structure
% Key: at this stage, node status is "informal" or "stated"
% Example: GMTNet project --- GMT textbook structured as obj/mor network

\subsection{Phase 2: Network Analysis \texorpdfstring{$\to$}{→} Formalization Strategy}
\label{sec:pipe-phase2}

% Betweenness centrality: identify bottleneck nodes
% PageRank: identify "infrastructure" nodes
% Community detection: identify independently formalizable modules
% Key insight: signals cannot be computed on leanblueprint's flat \uses graph,
% but are semantically rich on Astrolabe's typed mor network

\subsection{Phase 3: Directed Formalization}
\label{sec:pipe-phase3}

% Based on network analysis signals, selectively formalize critical nodes
% Formalization can be manual or AI-assisted (LLM sorry-filling)
% Astrolabe provides strategic guidance, does not intervene in formalization process

\subsection{Phase 4: Formalization Artifact Reflux}
\label{sec:pipe-phase4}

% .ilean compilation artifacts → ilean parser plugin → Astrolabe obj/mor
% Lean def → obj (sort: "lean-definition"), theorem → obj (sort: "lean-theorem")
% Type dependencies → mor (sort: "type-uses"), proof dependencies → mor (sort: "proof-uses")
% Closed loop: post-reflux network runs analysis again, evaluates coverage, identifies next batch

\subsection{Coexistence of Natural Language and Formalized Graphs: Three-Layer Design}
\label{sec:pipe-coexistence}

% Layer 1: Many-to-many formalization mapping (data model)
% - One NL obj → multiple Lean obj (theorem split into statement + lemma + helper)
% - One Lean obj → multiple NL obj (shared lemma)
% - Typed mor naturally supports many-to-many, no extra mechanism needed

% Layer 2: Sort-driven differential rendering (visual semantics)
% Table: mor sort → visual representation → semantic meaning
% inclusion → nesting, uses → solid arrow, proof-uses → dashed arrow,
% formalizes → double-line, generalizes → gradient arrow, contradicts → red line

% Layer 3: MDX components as knowledge reference DSL (content expression)
% Basic: ObjBlock, ObjRef, MorBlock, MorRef
% Composite (designed): NodeCluster, ProofTree, FormalizationStatus, DependencyDiff
% Analogy: obj/mor = data types, sort = type system, MDX components = syntax, plugins = stdlib, AI = code generator

\subsection{Fundamental Differences from Existing Workflows}
\label{sec:pipe-comparison}

% leanblueprint (linear): LaTeX → manual assignment → Lean → manual \leanok
% LeanArchitect (reverse): Lean code → auto-generate blueprint
% Astrolabe (closed-loop): NL → network → analysis → formalization → reflux → update → re-analyze
