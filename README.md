<p align="center">
  <a href="https://github.com/MathNetwork/Astrolabe">
    <img src="src-tauri/icons/128x128@2x.png" alt="Astrolabe Category" width="80" />
  </a>
</p>

<h1 align="center">Astrolabe Category</h1>

<p align="center">
  A Categorical Foundation for Knowledge Management
</p>

<p align="center">
  <a href="https://github.com/MathNetwork/Astrolabe/blob/main/LICENSE"><img alt="License: AGPL-3.0" src="https://img.shields.io/badge/license-AGPL--3.0-blue" /></a>
  <a href="https://github.com/MathNetwork/Astrolabe/releases/latest"><img alt="Release" src="https://img.shields.io/badge/release-v0.1.0-brightgreen" /></a>
</p>

<p align="center">
  <a href="https://github.com/MathNetwork/Astrolabe/releases/latest/download/Astrolabe_0.1.9_aarch64.dmg"><img alt="macOS Apple Silicon" src="https://img.shields.io/badge/macOS-Apple%20Silicon-000?logo=apple&logoColor=white" /></a>
  <a href="https://github.com/MathNetwork/Astrolabe/releases/latest/download/Astrolabe_0.1.9_x64.dmg"><img alt="macOS Intel" src="https://img.shields.io/badge/macOS-Intel-000?logo=apple&logoColor=white" /></a>
  <a href="https://github.com/MathNetwork/Astrolabe/releases/latest/download/Astrolabe_0.1.9_x64-setup.exe"><img alt="Windows" src="https://img.shields.io/badge/Windows-x64-0078D4?logo=windows&logoColor=white" /></a>
  <a href="https://github.com/MathNetwork/Astrolabe/releases/latest/download/Astrolabe_0.1.9_amd64.deb"><img alt="Linux" src="https://img.shields.io/badge/Linux-deb-FCC624?logo=linux&logoColor=black" /></a>
</p>

<p align="center">
  <a href="#astrolabe-category">Theory</a> |
  <a href="#structure">Structure</a> |
  <a href="#functors">Functors</a> |
  <a href="#build">Build</a>
</p>

---

## Astrolabe Category

Astrolabe category is a free category generated by a signature $\Sigma = (O, M, h)$, where objects and morphisms carry open information records and share a global identifier space.

An **astrolabe signature** $\Sigma = (O, M, h)$ consists of:

- $O$ — objects, each carrying an open information record
- $M$ — morphisms (first-class), each carrying source, target, and information record
- $h : O + M \to \text{Hex}_{12}$ — injective global identifier

The **astrolabe category** $\mathcal{A}(\Sigma) := \text{Free}(\Sigma)$ is the free category generated by $\Sigma$.

**This is an astrolabe category.** `backend/` is the signature $(O, M, h)$ and its functors, `formal/` is the specification that constrains them, `paper/` is the theory that defines them.

## Structure

```
astrolabe-category/
├── backend/
│   └── astrolabe/
│       ├── signature/                  # (O, M, h) — type definitions
│       │   ├── obj.py                  #   O — objects
│       │   ├── mor.py                  #   M — morphisms
│       │   └── state.py               #   runtime state
│       ├── signature_storage.py        # h — field-agnostic persistence
│       ├── functors/
│       │   ├── math_domain/            #   D : A(Σ) → A(Σ)  (name, sort, statement, proof)
│       │   ├── timestamp/              #   T : A(Σ) → A(Σ)  (created_at, updated_at)
│       │   ├── ilean_parser/           #   F_ilean : Lean → A(Σ)
│       │   ├── network_analysis/       #   A : A_M → A_M'
│       │   └── base.py                #   functor base class
│       └── server.py                   # API + functor pipeline
├── src/                                # functors to V, U_ui, P (Next.js)
│   ├── panels/                         #   P : A(Σ) → U_ui
│   ├── lib/graph2d.ts                  #   L : A(Σ) → P (d3-force)
│   ├── lib/sortConfig.ts               #   R : A(Σ) → V (color/size)
│   ├── stores/                         #   state management (zustand)
│   └── hooks/                          #   data loading, shortcuts
├── paper/                              # narrative (LaTeX)
├── formal/                             # specification (Lean 4, planned)
└── tests/                              # engineering tests (pytest + jest)
```

`signature_storage.py` is field-agnostic — it only manages $(O, M, h)$ CRUD. Field semantics (name, sort, statement, proof) are defined by functors, not hardcoded in storage.

## Functors

All operations on $\mathcal{A}(\Sigma)$ are **functors**:

| Functor | Signature | Role |
|---------|-----------|------|
| Math Domain | $D: \mathcal{A}(\Sigma) \to \mathcal{A}(\Sigma)$ | default fields (name, sort, status, statement, proof, notes) |
| Timestamp | $T: \mathcal{A}(\Sigma) \to \mathcal{A}(\Sigma)$ | created_at, updated_at |
| ilean Parser | $F_{\text{ilean}}: \mathbf{Lean} \to \mathcal{A}(\Sigma)$ | import .ilean declarations |
| Network Analysis | $A: \mathcal{A}\_{\mathcal{M}} \to \mathcal{A}\_{\mathcal{M}'}$ | centrality, communities, curvature |
| Rendering | $R: \mathcal{A}(\Sigma) \to \mathcal{V}$ | autoColor, sort rules |
| Presentation | $P: \mathcal{A}(\Sigma) \to \mathcal{U}\_{\text{ui}}$ | MDX components |
| Layout | $L: \mathcal{A}(\Sigma) \to \mathcal{P}$ | d3-force |

Creating an object: `signature_storage` generates $h$ → `math_domain` fills defaults → `timestamp` sets dates → user fields override.

## Formal Verification

`formal/` contains Lean 4 proofs that serve as **specification** in two senses:

- **Algebraic specification**: astrolabe signature + axioms define the category (cf. Goguen & Burstall 1992)
- **Software specification**: Lean proofs constrain the Python implementation

```
formal/     specification — what the code must satisfy (Lean 4)
tests/      engineering   — does the code run correctly (pytest + jest)
```

The formalization is imported back into astrolabe via $F_{\text{ilean}}$ — astrolabe manages the formal proofs of its own theory.

## Build

```bash
# frontend
npm install && npm run build

# backend
cd backend && pip install -r requirements.txt
python -m astrolabe.server

# formal verification
cd formal && lake build

# tests
npm test
cd backend && python -m pytest
```

## License

[AGPL-3.0](LICENSE) — free to use, modify, and distribute. Any derivative work or network service must also be open source.
